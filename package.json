{
  "name": "portchain",
  "version": "1.0.0",
  "description": "portchain code challange",
  "main": "build/app.js",
  "scripts": {
    "start": "tsc && node build/app.js",
    "lint": "biome check",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "@jobn",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "typescript": "^5.6.3"
  }
}
